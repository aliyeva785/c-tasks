#include <iostream>
using namespace std;

void f(int limit) {
    int a = 0, b = 1;
    while (a < limit) {
        cout << a << " ";
        int c = a + b;
        a = b;
        b = c;
    }
    cout << endl;
}
int main() {
    int limit = 1000;
    f(limit);
    return 0;
}
